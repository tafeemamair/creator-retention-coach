# MCP Retention Tool

This directory contains the MCP tool implementation for retention analysis.

Install dependencies with `npm install` and run with `npm run dev`.

Generated dependencies under `node_modules/` are intentionally not committed.
